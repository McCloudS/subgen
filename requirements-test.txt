# Test-only dependencies — does NOT include ML packages (stable-ts, faster-whisper, torch).
# Those are mocked in tests/conftest.py so CI stays fast and GPU-free.
pytest>=7.4
pytest-asyncio>=0.23
httpx>=0.25
requests-mock>=1.11
fastapi>=0.100
uvicorn>=0.24
requests>=2.31
python-multipart>=0.0.6
